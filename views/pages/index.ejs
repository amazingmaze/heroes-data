<!DOCTYPE html>
<html lang="en">
<head>
  <% include ../partials/head %>
</head>
<body class="container">

  <header>
    <% include ../partials/header %>
  </header>

  <main>
    <div class="row">
        <div class="jumbotron">
          <h3>Heroes</h3>
          <% heroes.forEach(function(hero) { %>
            <a class="hero" href='/<%= hero.name %>'><%= hero.name %></a>
          <% }); %>
        </div>
    </div>

  </main>

  <footer>
    <% include ../partials/footer %>
  </footer>

</body>
</html>
